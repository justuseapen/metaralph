# Ralph Progress Log
Started: Mon Jan 12 17:23:45 EST 2026
---

## Codebase Patterns
- Database operations use optional `db` parameter; if not provided, creates and closes connection
- Use `uuidv4()` for generating IDs
- Use ES module imports with `.js` extension even for `.ts` files
- Row-to-model conversion: database uses snake_case, models use camelCase
- Database migrations use `ALTER TABLE ADD COLUMN IF NOT EXISTS` pattern via PRAGMA table_info check
- Index naming convention: `idx_<table>_<column>`

---

## 2026-01-12 18:45 - US-010
- What was implemented:
  - Created src/queue/task.ts with Task interface and TaskRepository
  - Created src/queue/prioritizer.ts with calculatePriorityScore() and categorizeTask()
  - Updated src/db/index.ts with extended task schema and migration function
- Files changed:
  - src/queue/task.ts (new)
  - src/queue/prioritizer.ts (new)
  - src/db/index.ts (modified)
- **Learnings for future iterations:**
  - Task types: bug_fix, test, docs, refactor, feature (with scores 100, 90, 85, 70, 50)
  - Effort multipliers: quick_win=2.0, small=1.5, medium=1.0, large=0.5
  - Auto-approve logic: (bug_fix OR docs OR test) AND (quick_win OR small)
  - Task statuses: pending, approved, queued, running, completed, failed
  - TaskRepository methods: create(), findById(), findPending(), getNextTasks(), updateStatus(), updateApprovalStatus(), findByProject(), updatePrdJson()
---

## 2026-01-12 19:30 - US-011
- What was implemented:
  - Created src/queue/approval.ts with ApprovalQueue class and categorizeTask() re-export
  - ApprovalQueue has: submit(), approve(), reject(), getPending() methods
  - Updated src/cli/index.ts with queue commands: 'metaralph queue', 'metaralph approve <id>', 'metaralph reject <id>'
  - Queue display shows tasks grouped by approval status
- Files changed:
  - src/queue/approval.ts (new)
  - src/cli/index.ts (modified)
- **Learnings for future iterations:**
  - categorizeTask() is in prioritizer.ts; approval.ts re-exports it for API clarity
  - ApprovalQueue follows same db pattern as TaskRepository (optional db param)
  - CLI commands can be top-level (approve/reject) or nested (queue list)
  - getProject() imported from registry for task display
---

## 2026-01-12 20:00 - US-012
- What was implemented:
  - Created src/workers/execution.ts with Execution interface and ExecutionRepository
  - Created src/workers/ralph-spawner.ts with RalphSpawner class
  - RalphSpawner.spawn(task, project) creates prd.json and spawns ralph.sh subprocess
  - RalphSpawner.monitor(execution, childProcess) tracks process and collects output
  - Detects '<promise>COMPLETE</promise>' for success
- Files changed:
  - src/workers/execution.ts (new)
  - src/workers/ralph-spawner.ts (new)
- **Learnings for future iterations:**
  - Execution statuses: pending, running, completed, failed
  - ExecutionRepository methods: create(), update(), findById(), findRunning(), findByTask(), findByProject()
  - RalphSpawner.spawn() returns { success, execution, childProcess, error? }
  - RalphSpawner.monitor() returns Promise<CompletionResult> with output, exitCode, iterationsUsed
  - findRalphScript() searches project-local and user-level locations for ralph.sh
  - Child process spawned with stdio: ['ignore', 'pipe', 'pipe'] to capture output
---

## 2026-01-13 - US-013
- What was implemented:
  - Created src/workers/failure-handler.ts with retry logic:
    - MAX_RETRIES = 2, exponential backoff (5s base, 10s for retry 2)
    - handleFailure() determines if retry should happen
    - FailureHandler class wraps functional API
  - Created src/workers/index.ts with WorkerOrchestrator class:
    - Enforces maxConcurrentWorkers from config
    - One worker per project via projectLocks Set
    - scheduleTask() checks capacity, spawns worker via RalphSpawner
    - monitorWorker() handles completion, triggers retries on failure
    - Emits events: worker:started, worker:completed, worker:failed, worker:retry
  - Updated 'metaralph workers' CLI to show active workers from ExecutionRepository.findRunning()
- Files changed:
  - src/workers/failure-handler.ts (new)
  - src/workers/index.ts (new)
  - src/cli/index.ts (modified)
- **Learnings for future iterations:**
  - WorkerOrchestrator uses in-memory Maps/Sets for activeWorkers and projectLocks
  - Retry counts are stored in-memory (resets on daemon restart)
  - WorkerOrchestrator extends EventEmitter for events
  - scheduleTask() returns ScheduleResult { success, message, worker? }
  - ActiveWorker contains: task, project, execution, childProcess, startedAt, completionPromise
  - Exponential backoff formula: BASE_DELAY * 2^(retryNumber - 1)
---

## 2026-01-13 - US-014
- What was implemented:
  - Created src/queue/index.ts with QueueManager class:
    - start() begins scheduling loop running every 5 seconds (SCHEDULING_INTERVAL_MS = 5000)
    - stop() and stopAll() for graceful shutdown
    - scheduleNextTasks() checks capacity, gets next tasks, schedules via WorkerOrchestrator
    - Emits events: 'task:scheduled', 'task:completed', 'task:failed'
    - Re-exports all queue module exports (Task, TaskRepository, ApprovalQueue, etc.)
  - Created src/daemon/runner.ts as the daemon process entry point:
    - Initializes QueueManager and starts scheduling loop
    - Sets up event listeners for logging
    - Handles SIGTERM/SIGINT for graceful shutdown
  - Updated src/daemon/index.ts to spawn runner.ts instead of inline script
- Files changed:
  - src/queue/index.ts (new)
  - src/daemon/runner.ts (new)
  - src/daemon/index.ts (modified)
- **Learnings for future iterations:**
  - QueueManager extends EventEmitter like WorkerOrchestrator
  - QueueManager wraps WorkerOrchestrator (composition pattern)
  - scheduleNextTasks() updates task status to 'running' before scheduling
  - Daemon lifecycle: startDaemon() spawns runner.js as detached process
  - Runner module checks process.argv to detect direct execution
  - QueueManager tracks stats: tasksScheduled, tasksCompleted, tasksFailed, lastScheduleCheck
---

## 2026-01-13 - US-015
- What was implemented:
  - Created src/cli/dashboard.tsx with Ink-based terminal UI:
    - Dashboard component with tab navigation: Queue, Approvals, Projects, Workers
    - Header component showing version and daemon status (refreshes every 2s)
    - TabBar component with [1] [2] [3] [4] shortcut indicators
    - TabContent placeholder component (will be implemented in US-016/US-017)
    - Footer component showing keyboard shortcuts
    - useInput hook handles keyboard: q quits, 1-4 switches tabs
  - Updated src/cli/index.ts:
    - Added 'metaralph dashboard' command
    - Added default action so 'metaralph' (no args) launches dashboard
- Files changed:
  - src/cli/dashboard.tsx (new)
  - src/cli/index.ts (modified)
- **Learnings for future iterations:**
  - Ink uses React 18 with hooks (useState, useEffect, useInput, useApp)
  - useApp().exit() exits the Ink app
  - Tab type uses discriminated union pattern with TabId
  - getDaemonStatus() imported from daemon/index.js for header display
  - Package version read from package.json via fileURLToPath
  - TSX files work due to "jsx": "react-jsx" in tsconfig.json
---

## 2026-01-13 - US-016
- What was implemented:
  - Created src/cli/components/QueueView.tsx showing task queue:
    - Table with columns: Priority, ID, Project, Type, Status, Title
    - Auto-refreshes every 2 seconds via useEffect interval
    - Uses TaskRepository.findPending() for data
    - Shows loading state and empty state messages
  - Created src/cli/components/ApprovalView.tsx for approval management:
    - Split view: task list on left (40%), details panel on right (60%)
    - Arrow keys (up/down) navigate between pending tasks
    - 'a' key approves selected task, 'r' key rejects
    - Shows full task details: ID, Title, Project, Type, Effort, Priority, Source, Description
    - Success/error messages with 3-second auto-dismiss
    - Uses ApprovalQueue class for approve/reject operations
  - Updated src/cli/dashboard.tsx:
    - Integrated QueueView and ApprovalView components
    - TabContent now uses switch statement to render appropriate view
- Files changed:
  - src/cli/components/QueueView.tsx (new)
  - src/cli/components/ApprovalView.tsx (new)
  - src/cli/dashboard.tsx (modified)
- **Learnings for future iterations:**
  - Ink components go in src/cli/components/ directory
  - useInput hook is used for keyboard handling in approval view
  - Views should auto-refresh with setInterval + useEffect pattern
  - useCallback for functions referenced in useEffect dependencies
  - Task display helpers: formatStatus(), formatType(), formatEffort(), truncate()
  - Split layouts use Box with percentage widths (width="40%")
  - Border styles: 'single', 'round' for different visual treatments
---

## 2026-01-13 - US-017
- What was implemented:
  - Created src/cli/components/ProjectsView.tsx for project management:
    - Shows projects in a table with: Name, Health Score, Tasks count, Path
    - Health score calculated from completed vs failed task ratio (0-100%)
    - Selected project shows details panel on right side
    - 'a' key opens text input to add new project by path
    - 'd' key deletes project with confirmation (press twice)
    - Arrow keys (up/down) navigate between projects
    - Auto-refreshes every 2 seconds
    - Uses ink-text-input for path input
  - Created src/cli/components/WorkersView.tsx for worker monitoring:
    - Summary stats bar showing running/completed/failed counts
    - Worker cards show: project name, task title, progress bar, elapsed time
    - Progress bar based on iterations used (iterations/10 max)
    - Last 3 output lines displayed for each worker
    - Empty state with instructions for starting daemon and creating tasks
    - Auto-refreshes every 2 seconds
  - Updated src/cli/dashboard.tsx to integrate ProjectsView and WorkersView
  - Added ink-text-input dependency for text input in ProjectsView
- Files changed:
  - src/cli/components/ProjectsView.tsx (new)
  - src/cli/components/WorkersView.tsx (new)
  - src/cli/dashboard.tsx (modified)
  - package.json (modified - added ink-text-input)
  - package-lock.json (modified)
- **Learnings for future iterations:**
  - ink-text-input provides TextInput component for text input in Ink
  - Health score calculation: (completed / (completed + failed)) * 100
  - Confirmation patterns: track confirmDeleteId state, require two presses
  - Worker progress display: use ExecutionRepository.findRunning() for active workers
  - Resolve task and project from execution for display context
  - formatElapsed() helper for human-readable time since startedAt
  - Progress bars: use '█' for filled and '░' for empty segments
---

## 2026-01-13 - US-018
- What was implemented:
  - Created src/onboarding/analyzer.ts with CodebaseAnalyzer class:
    - analyze(projectPath) returns ImprovementOpportunity[] array
    - detectOutdatedDeps() runs `npm outdated --json` and parses results
    - detectTypeErrors() runs `tsc --noEmit` and parses error output
    - detectLintIssues() runs `eslint . --format json` if eslint config exists
    - runCommand() helper spawns child processes and captures stdout/stderr
    - hasPackageJson(), hasTsConfig(), hasEslintConfig() detect available tools
    - commandExists() checks for local node_modules/.bin commands
  - Created src/onboarding/todo-extractor.ts with TodoExtractor:
    - extract(projectPath) returns TodoComment[] with tag, content, file, line
    - extractAsOpportunities() converts TODOs to ImprovementOpportunity[]
    - getSummary() returns counts by TODO/FIXME/XXX/HACK tag
    - Scans common file extensions, skips node_modules/dist/build etc.
    - tagToTaskType() maps FIXME->bug_fix, HACK->refactor, TODO/XXX->feature
    - estimateEffort() keyword-based effort estimation from comment content
- Files changed:
  - src/onboarding/analyzer.ts (new)
  - src/onboarding/todo-extractor.ts (new)
- **Learnings for future iterations:**
  - ImprovementOpportunity interface: type, title, description, file?, line?, severity, suggestedTaskType, suggestedEffort
  - OpportunityType: 'outdated_dep' | 'type_error' | 'lint_issue' | 'todo_comment'
  - TodoExtractor tags: TODO, FIXME, XXX, HACK (searched with regex in comments)
  - Severity mapping: FIXME=high, HACK/XXX=medium, TODO=low
  - npm outdated --json returns {pkgName: {current, wanted, latest, dependent, location}}
  - TypeScript error format: path/file.ts(line,col): error TS####: message
  - ESLint JSON format: [{filePath, messages: [{line, column, severity, message, ruleId}]}]
  - SKIP_DIRS for file scanning: node_modules, .git, dist, build, out, etc.
  - MAX_FILE_SIZE = 1MB to avoid memory issues on large files
---

## 2026-01-13 - US-019
- What was implemented:
  - Created src/onboarding/proposal-generator.ts with ProposalGenerator:
    - generate() creates Task objects from ImprovementOpportunity findings
    - createTask() for single opportunity to task conversion
    - preview() to preview proposals without creating tasks
    - Generates PRD JSON with acceptance criteria based on opportunity type
    - Returns ProposalGenerationResult with summary statistics
  - Created src/onboarding/index.ts with OnboardingEngine:
    - onboard(projectPath) - full pipeline: validate git -> analyze -> extract TODOs -> generate proposals
    - analyze(projectId) - re-analyze existing project by ID
    - preview(projectPath) - analysis without creating tasks
    - Creates initial conversation with analysis summary
    - Updates project.lastAnalyzed timestamp
  - Added last_analyzed column to projects table via migration
  - Added CLI command: 'metaralph projects analyze <id>' with --preview option
- Files changed:
  - src/onboarding/proposal-generator.ts (new)
  - src/onboarding/index.ts (new)
  - src/db/index.ts (modified - added migrateProjectsTable)
  - src/cli/index.ts (modified - added analyze command)
- **Learnings for future iterations:**
  - ProposalGenerator creates PRD JSON with userStories structure matching ralph.sh expectations
  - Proposal interface: id, projectId, opportunity, taskInput, createdAt
  - OnboardingEngine uses isGitRepository() from registry to validate projects
  - Conversations and messages are stored in DB for project onboarding summaries
  - migrateProjectsTable follows same pattern as migrateTasksTable for column additions
  - CLI async actions supported by commander - use async/await in action handler
  - getProjectById aliased import pattern to avoid naming conflicts with getProject
---

## 2026-01-13 - US-020
- What was implemented:
  - Created src/collaboration/conversation.ts with Conversation and Message models:
    - Conversation interface with: id, projectId, title, status, createdAt, updatedAt
    - Message interface with: id, conversationId, role (user/assistant/system), content, createdAt
    - ConversationRepository with: create(), findById(), findByProject(), findActive()
    - Message operations: addMessage(), getMessages(), getWithMessages()
    - Status/title updates: updateStatus(), updateTitle(), delete()
  - Created src/collaboration/discussion.ts with Claude-powered discussion engine:
    - DiscussionEngine.respond() generates contextual responses in a conversation
    - DiscussionEngine.startConversation() creates new conversation with initial response
    - Builds project context from README, directory structure, package.json
    - Uses @anthropic-ai/sdk for Claude API integration (claude-sonnet-4-20250514)
    - System prompt includes project context for intelligent, project-aware responses
  - Added @anthropic-ai/sdk dependency for Claude integration
- Files changed:
  - src/collaboration/conversation.ts (new)
  - src/collaboration/discussion.ts (new)
  - package.json (modified - added @anthropic-ai/sdk)
  - package-lock.json (modified)
- **Learnings for future iterations:**
  - Collaboration module lives in src/collaboration/ directory
  - Conversation and Message use same row-to-model pattern as other repositories (snake_case to camelCase)
  - DiscussionEngine requires ANTHROPIC_API_KEY env variable for Claude API
  - Project context includes: README (truncated to 5000 chars), directory structure (2 levels), package.json
  - Claude messages format: filter out 'system' role, keep only 'user' and 'assistant'
  - getDirectoryStructure() uses tree-like format with └── and ├── connectors
  - Discussion responses are automatically stored as messages in the conversation
---

## 2026-01-13 - US-021
- What was implemented:
  - Created src/collaboration/prd-builder.ts with PrdBuilder class for interactive PRD creation:
    - PrdBuilder.start() begins a new PRD building session with Claude
    - PrdBuilder.continue() continues an existing session with user messages
    - PrdBuilder.buildPrd() generates complete PRD from description using AI
    - createTaskFromPrd() auto-creates Task when PRD is finalized
    - Parses PRD JSON from Claude response using <prd></prd> tags
    - In-memory session store (activeSessions Map) tracks ongoing PRD conversations
    - Prd and UserStory interfaces match Ralph's expected prd.json format
  - Created src/cli/chat.ts with interactive terminal chat implementation:
    - startChatSession() opens interactive readline-based chat with MetaRalph
    - Detects PRD-related requests and switches to PrdBuilder mode
    - Falls back to DiscussionEngine for general questions
    - Auto-displays task details when PRD is finalized
  - Added startProposeSession() for automated improvement proposals:
    - Runs OnboardingEngine.preview() for concrete analysis
    - Uses Claude to generate higher-level improvement proposals
    - Displays analysis summary and proposal recommendations
  - Updated src/cli/index.ts with new CLI commands:
    - 'metaralph chat <project>' opens interactive chat session
    - 'metaralph propose <project>' with --auto-queue option
    - Both commands accept project ID or name
- Files changed:
  - src/collaboration/prd-builder.ts (new)
  - src/cli/chat.ts (new)
  - src/cli/index.ts (modified - added chat and propose commands)
- **Learnings for future iterations:**
  - PrdBuilder uses session-based approach with in-memory Map for active sessions
  - Claude response parsing: look for <prd></prd> tags or raw JSON with "project" key
  - Interactive CLI uses node:readline createInterface for terminal input
  - Task source for PRD-created tasks is 'conversation' (vs 'onboarding' for analysis)
  - inferTaskType() maps keywords like 'bug'/'fix' -> bug_fix, 'test' -> test, etc.
  - inferEffortLevel() based on story count: 1=quick_win, 2-3=small, 4-5=medium, 6+=large
  - PRD_BUILDER_SYSTEM_PROMPT emphasizes small tasks, verifiable criteria, proper sequencing
  - Chat session cleanup: handle SIGINT, close readline on exit
---

## 2026-01-13 - US-022
- What was implemented:
  - Created src/learning/learning.ts with Learning model and LearningRepository:
    - Learning interface with: id, executionId, projectId, category, content, confidence, timesApplied, timesSuccessful, tags
    - LearningCategory types: pattern, gotcha, dependency, testing, architecture, workflow, environment, general
    - LearningRepository with: create(), findById(), findAll(), findByProject(), findByCategory(), findByExecution(), search(), update(), recordApplication(), delete(), findHighConfidence()
    - Metadata (tags, timesSuccessful) stored in content field with ---METADATA--- separator
    - recordApplication() recalculates confidence based on success rate formula
  - Created src/learning/cross-project.ts with CrossProjectPatternExtractor:
    - extractFromExecution(executionId) parses progress.txt for patterns
    - extractPatternsFromContent() parses "Codebase Patterns" and "Learnings for future iterations" sections
    - inferCategory() maps content keywords to LearningCategory
    - extractTags() extracts technology-related tags from content
    - Similarity-based deduplication to avoid duplicate learnings
    - extractFromProject() processes all completed executions for a project
    - extractFromProgressFile() for direct file processing
  - Created src/learning/outcome-tracker.ts with OutcomeTracker:
    - analyzeOutcomes() correlates execution outcomes with task attributes (type, effort, source)
    - AttributeCorrelation tracks success rates per attribute value
    - OutcomeInsight generates high_success, low_success, trend, and recommendation insights
    - createLearningsFromAnalysis() creates Learning objects from outcome insights
    - getHighSuccessTaskTypes() and getHighSuccessEffortLevels() for quick queries
- Files changed:
  - src/learning/learning.ts (new)
  - src/learning/cross-project.ts (new)
  - src/learning/outcome-tracker.ts (new)
- **Learnings for future iterations:**
  - Learning module lives in src/learning/ directory
  - Metadata stored in content field using ---METADATA--- separator (avoids schema changes)
  - LearningRepository follows same optional db parameter pattern as other repositories
  - Confidence recalculation formula: min(1.0, 0.5 + (successRate * 0.5))
  - Pattern extraction uses regex to find "Codebase Patterns" and "Learnings for future iterations" sections
  - Similarity check uses word overlap ratio (threshold 0.7) to detect duplicate patterns
  - OutcomeTracker minSampleSize default is 3 for statistical significance
  - Insight confidence increases with sample size: min(0.9, 0.5 + (samples/20) * 0.4)
---

## 2026-01-13 - US-023
- What was implemented:
  - Created src/learning/index.ts with LearningSystem class:
    - LearningSystem.findApplicableLearnings(task) finds relevant learnings by context matching
    - Context matching uses: project match, category relevance, tag overlap, content keywords, confidence
    - LearningSystem.injectLearnings(task, learnings) adds learnings to progress.txt Codebase Patterns section
    - recordOutcome(taskId, wasSuccessful) updates learning success metrics after task execution
    - getAppliedLearnings(taskId) retrieves which learnings were applied for tracking
    - extractFromExecution() and analyzeAndCreateLearnings() delegate to submodules
  - Re-exports all types from learning.ts, cross-project.ts, and outcome-tracker.ts
  - Singleton instance exported as learningSystem
- Files changed:
  - src/learning/index.ts (new)
- **Learnings for future iterations:**
  - LearningSystem uses applicationRecords Map to track which learnings were applied to which tasks
  - Relevance scoring uses weighted sum: project(3), category(2), tags(2), content(2), confidence(1)
  - Minimum relevance threshold is 0.3 (learnings below this are not applied)
  - Max 10 learnings are applied per task to avoid overwhelming context
  - Learnings are injected into progress.txt Codebase Patterns section (not CLAUDE.md)
  - Category relevance map determines which learning types are most useful for which task types
  - Tag extraction looks for common tech keywords (typescript, react, etc.)
  - Content relevance uses keyword extraction with stop word filtering
---
