# Ralph Progress Log
Started: Wed Jan 14 20:48:19 EST 2026
---

## Codebase Patterns
- Dashboard tabs are defined in `src/cli/dashboard.tsx` in the `TABS` array with `TabId` type union
- Tab views are React components in `src/cli/components/` named `*View.tsx`
- Footer component shows keyboard shortcuts - update when adding new tabs
- Ink library used for terminal UI - Box, Text, useInput from 'ink'
- Ink's Key type has: upArrow, downArrow, leftArrow, rightArrow, pageUp, pageDown, return, escape, ctrl, shift, tab, backspace, delete, meta
- Use `Text inverse={selected}` for selection highlighting (not backgroundColor)
- Database access via *Repository classes in `src/collaboration/` and `src/queue/`
- 2-second refresh interval for real-time dashboard updates
- Anthropic SDK streaming: use `client.messages.stream()` and `stream.on('text', callback)`
- For spinners in Ink, use text-based frames (e.g., SPINNER_FRAMES array) with setInterval

---

## 2026-01-14 - US-101
- What was implemented: Added Chat tab to dashboard navigation
- Files changed:
  - `src/cli/dashboard.tsx` - Added 'chat' to TabId, added Chat tab to TABS array, added ChatView to TabContent switch, updated Footer
  - `src/cli/components/ChatView.tsx` - Created new component (placeholder)
- **Learnings for future iterations:**
  - Tab navigation is handled automatically by useInput hook that reads from TABS array
  - Each tab needs: TabId type member, TABS array entry, TabContent case, Footer shortcut
  - View components follow pattern: export function XView(): React.ReactElement
---

## 2026-01-14 - US-102
- What was implemented: Enhanced ChatView component with full message display functionality
- Files changed:
  - `src/cli/components/ChatView.tsx` - Complete rewrite with:
    - Message history display with timestamps (formatted dates/times)
    - Role-specific styling (user=cyan, assistant=green, system=yellow)
    - Arrow key scrolling with auto-scroll to bottom on new messages
    - Loading state and empty state handling
    - Conversation list sidebar for multiple conversations
    - Date grouping headers (Today, Yesterday, dates)
- **Learnings for future iterations:**
  - ConversationRepository.findActive() gets active conversations
  - ConversationRepository.getMessages(convId) gets messages for a conversation
  - Ink's Key type doesn't include 'end' or 'home' - use input string checks instead (e.g., 'g' for start, 'G' for end)
  - Use Text inverse={selected} for selection highlighting, not backgroundColor (Box doesn't have that prop)
  - 2-second refresh interval is the standard pattern for dashboard views
---

## 2026-01-14 - US-103
- What was implemented: Added chat input with multi-line support to ChatView
- Files changed:
  - `src/cli/components/ChatView.tsx` - Added:
    - ChatInput component with ink-text-input for message entry
    - Ctrl+Enter or Cmd+Enter (key.meta) submits the prompt
    - Escape clears current input
    - Character count displayed (e.g., '150/4000')
    - Input disabled state while waiting for response
    - MAX_INPUT_LENGTH constant (4000 chars)
    - isProcessing state for managing disabled state
    - handleSubmitMessage placeholder (actual API integration in US-104)
- **Learnings for future iterations:**
  - ink-text-input is already available in package.json
  - TextInput from 'ink-text-input' handles basic text input with Enter to submit
  - Use key.meta for Cmd key on Mac, key.ctrl for Ctrl key
  - key.return is used for Enter key in Ink
  - For simulating async operations, useState with setTimeout works for placeholder behavior
  - Input appears in both empty state and main view to allow starting conversations
---

## 2026-01-14 - US-104
- What was implemented: Streaming Claude responses in chat with real-time display
- Files changed:
  - `src/cli/components/ChatView.tsx` - Major enhancements:
    - Streaming response integration using Anthropic SDK `client.messages.stream()`
    - StreamingIndicator component with animated spinner (⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏)
    - StreamingMessage interface to track streaming state
    - Token-by-token response display as stream.on('text') events fire
    - Auto-scroll that follows new streaming content
    - Scroll-up pauses auto-scroll, scroll to bottom or 'G' re-enables
    - Streaming status indicator in header showing "⠋ Streaming..."
    - Auto-scroll paused indicator shown when paused
- **Learnings for future iterations:**
  - Anthropic SDK has `client.messages.stream()` for streaming responses
  - MessageStream emits 'text' event with (textDelta, textSnapshot) - use textDelta to accumulate
  - Use `stream.finalMessage()` to await completion and get full response
  - Use `stream.on('text', callback)` to handle streaming text deltas
  - For spinner animation, use setInterval with ~100ms delay and cycle through frames
  - Track auto-scroll state separately - disable on user scroll up, re-enable at bottom
  - Use useRef for AbortController to allow stream cancellation
  - Keep message history for context (slice last 10 messages to avoid context limits)
---

## 2026-01-14 - US-105
- What was implemented: Project context selector for chat
- Files changed:
  - `src/cli/components/ChatView.tsx` - Added:
    - ProjectSelector component with dropdown/selector UI
    - 'p' key opens/closes project selector
    - Arrow keys navigate the project list
    - Enter confirms selection, Escape cancels
    - "No project" option at index 0
    - Selected project indicator shown in header (e.g., "[metaralph]")
    - Project context now comes from selected project, not conversation
    - Auto-select project from conversation on load if no manual selection
    - Navigation hints updated to include 'p' shortcut
- **Learnings for future iterations:**
  - listProjects() returns all registered projects from the registry
  - Keep both projectMap (for lookups) and projectList (for iteration) for different use cases
  - Use separate state for selector: selectedProjectId, projectSelectorOpen, projectSelectorIndex
  - Block other key handlers when selector is open (return early after handling)
  - Use useCallback for selection handler to avoid stale closures
  - Project context is passed to buildSystemPrompt() which includes project name, path, README, and structure
---

## 2026-01-14 - US-106
- What was implemented: Added Loops tab to dashboard navigation
- Files changed:
  - `src/cli/dashboard.tsx` - Added 'loops' to TabId, added Loops tab to TABS array with shortcut '0', added LoopsView to TabContent switch, updated Footer
  - `src/cli/components/LoopsView.tsx` - Created new component with placeholder content
- **Learnings for future iterations:**
  - Adding a new tab requires 4 changes: TabId type, TABS array, TabContent case, Footer shortcut
  - Loops tab uses shortcut '0' (zero) - consistent with pattern of single-digit shortcuts
  - LoopsView follows same pattern as other views: export function XView(): React.ReactElement
---

## 2026-01-14 - US-107
- What was implemented: Created loops and loop_iterations database tables
- Files changed:
  - `src/db/index.ts` - Added two new table definitions in createTables():
    - `loops` table: id, project_id, branch_name, prd_path, status, max_iterations, current_iteration, started_at, completed_at, created_at
    - `loop_iterations` table: id, loop_id, iteration_number, story_id, status, output, commit_sha, started_at, completed_at
    - Added 4 indexes: idx_loops_project_id, idx_loops_status, idx_loop_iterations_loop_id, idx_loop_iterations_status
- **Learnings for future iterations:**
  - Database tables use CREATE TABLE IF NOT EXISTS for idempotent creation
  - Foreign keys reference projects(id) and loops(id) with ON DELETE CASCADE
  - Status columns use TEXT type with DEFAULT values (status tracking: pending/running/paused/completed/failed/stopped)
  - Indexes follow naming pattern: idx_{table}_{column}
  - No separate migration files - all schema lives in src/db/index.ts createTables()
---
